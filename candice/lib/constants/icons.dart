import 'package:flutter/material.dart';

import 'colors.dart';

const Icon kCommentIcon = Icon(Icons.mode_comment, color: Colors.blue);
const Icon kLikeIcon = Icon(Icons.favorite, color: kPink);
const Icon kNotLikeIcon = Icon(Icons.favorite_border, color: Colors.white);
